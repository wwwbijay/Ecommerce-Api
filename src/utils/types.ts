export type CreateUserParams = {
    username: string;
    name: string;
    email: string;
    password: string;
}

export type UpdateUserParams = {
    username: string;
    name: string;
    email: string;
    password: string;
}